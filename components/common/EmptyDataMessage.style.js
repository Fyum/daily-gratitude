import themeStyle from '../../themes/styles';
const {
  textColor,
} = themeStyle;


export
  const _view = {
    alignSelf: 'center',
    justifyContent: 'center',
    height: 200
  }

export
  const _text = {
    color: textColor,
    borderWidth: 2,
    borderColor: textColor,
    borderRadius: 10,
    padding: 20,
    fontSize: 17,
  }